{% extends "base.html.twig" %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Modifier la catégorie {{ category.label }}</h4>
            </div>
        </div>
        {% include "includes/category_form.html.twig" %}
    </div>

    <section>
        <div>
            {% for category in categories %}
                <span style="background-color: {{ category.color }}">
                    {{ category.label }}
                </span>
            {% endfor %}
        </div>
    </section>

    {% block javascripts %}
        {{ parent() }}
        {{ encore_entry_script_tags('categories') }}
    {% endblock %}
{% endblock %}