{% apply inline_css(source('@styles/email.css')) %}
<h1>Nouveau ? Confirmez votre email !</h1>

<p>Vous venez de souscrire au blog de XipelGame, <a href="{{ signedUrl|raw }}">confirmez votre email</a>.</p>
<p>Ce lien expire dans {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.</p>

<p>A bientôt parmis-nous !</p>

    <!--
<div class="social">
    <a href="facebook.com">
        <img src="{{ email.image('@images/twitter.svg') }}" alt="Logo">
    </a>
    <a href="google.com">
        <img src="{{ email.image('@images/facebook.svg') }}" alt="Logo">
    </a>
    <a href="google.com">
        <img src="{{ email.image('@images/instagram.svg') }}" alt="Logo">
    </a>
</div> -->
{% endapply %}